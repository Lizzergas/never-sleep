/**
 * Adds Compose Multiplatform to a module. Apply together with
 * template.kmp.library (or use template.kmp.feature which bundles both).
 */
plugins {
    id("org.jetbrains.compose")
    id("org.jetbrains.kotlin.plugin.compose")
}
