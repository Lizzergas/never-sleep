---
interface Props {
    title: string;
    description?: string;
}

const { title, description } = Astro.props;
---

<div class="section-heading">
    <h2>{title}</h2>
    {description ? <p class="section-heading__lede">{description}</p> : null}
</div>